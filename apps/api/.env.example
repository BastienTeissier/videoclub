PORT=3001
DATABASE_URL=postgresql://videoclub:videoclub@localhost:5432/videoclub

# AI Provider
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash-preview
USE_LOCAL_MODEL=false
OLLAMA_BASE_URL=http://localhost:11434
LOCAL_MODEL_NAME=llama3

# A2UI demo mode — milliseconds to sleep between A2UI frames during progressive rendering.
# Leave empty in production. Set to 700 during demo rehearsals to make staging visible.
DEMO_MODE_SLEEP=
